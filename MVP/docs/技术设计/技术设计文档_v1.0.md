## 文档变更记录

| 版本 | 日期 | 修改人 | 修改说明 |
|-----|------|--------|---------|
| v1.0 | 2026-04-15 17:45 | Claude | 初始版本，技术架构设计 |

---

# MVP 新闻爬虫与 AI 整理系统 - 技术设计文档

## 1. 架构概述

### 1.1 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Scheduler (APScheduler)              │
│                    (定时任务调度，可配置时间点)             │
└────────────────────────────┬────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│   Crawlers    │   │   Storage     │   │   Notifiers   │
│  (多网站爬虫)  │   │  (SQLite)    │   │ (飞书/邮件)   │
└───────┬───────┘   └───────────────┘   └───────────────┘
        │
        ▼
┌───────────────┐
│     LLM       │
│  (OpenAI API) │
└───────────────┘
```

### 1.2 核心设计原则

1. **模块化设计**：各功能模块独立，通过接口交互
2. **可扩展性**：基于基类抽象，易于扩展新的爬虫、通知渠道、LLM 提供商
3. **配置驱动**：所有关键配置通过配置文件管理
4. **可测试性**：核心逻辑独立，便于单元测试

## 2. 模块详细设计

### 2.1 爬虫模块 (Crawlers)

**基类设计**：
```python
class BaseCrawler(ABC):
    name: str
    base_url: str
    source_name: str

    @abstractmethod
    def fetch() -> List[NewsItem]:
        pass
```

**扩展方式**：
- 继承 `BaseCrawler`
- 实现 `fetch()` 方法
- 在 `CRAWLER_REGISTRY` 中注册

### 2.2 通知模块 (Notifiers)

**基类设计**：
```python
class BaseNotifier(ABC):
    name: str

    @abstractmethod
    def send(news_list: List[NewsItem], title: str) -> bool:
        pass
```

**扩展方式**：
- 继承 `BaseNotifier`
- 实现 `send()` 方法

### 2.3 大模型模块 (LLM)

**基类设计**：
```python
class BaseLLM(ABC):
    name: str

    @abstractmethod
    def chat(messages: List[dict]) -> Optional[str]:
        pass

    @abstractmethod
    def batch_chat(prompts: List[str]) -> List[Optional[str]]:
        pass

    def process_news(news_list: List[NewsItem]) -> List[NewsItem]:
        # 去重、评分、排序
        pass
```

**预留 RAG 接口**：
- 未来可在 `process_news()` 中接入 RAG 检索
- 支持多 RAG 源配置

### 2.4 存储模块 (Storage)

**数据模型**：
- 使用 SQLAlchemy ORM
- SQLite（MVP）→ PostgreSQL（后续）
- Repository 模式封装数据访问

## 3. 数据流程

```
1. 定时触发
   ↓
2. 执行所有爬虫 → 收集 NewsItem 列表
   ↓
3. 按发布时间排序
   ↓
4. 存储到数据库
   ↓
5. 飞书通知（原始新闻）
   ↓
6. LLM 处理（去重、AI 相关度评分、排序）
   ↓
7. 邮件通知（整理后的新闻）
```

## 4. 配置文件结构

```yaml
scheduler:
  cron_times: ["08:30", "11:30", "17:30"]
  timezone: "Asia/Shanghai"

crawlers:
  enabled: ["jqzx", "kr36", "aiera", "radar", "qbit"]
  timeout: 30
  request_delay: 2.0

database:
  path: "./data/news.db"

llm:
  base_url: "https://api.openai.com/v1"
  api_key: "..."
  model: "gpt-4"

feishu:
  webhook_url: "..."
  enabled: true

email_163:
  sender: "..."
  password: "..."
  recipients: []
  enabled: true
```

## 5. 扩展性设计

### 5.1 新增爬虫网站

1. 在 `crawlers/` 下创建新文件
2. 继承 `BaseCrawler`
3. 实现 `fetch()` 方法
4. 在 `crawlers/__init__.py` 的 `CRAWLER_REGISTRY` 中注册
5. 在配置文件 `crawlers.enabled` 中启用

### 5.2 新增通知渠道

1. 在 `notifiers/` 下创建新文件
2. 继承 `BaseNotifier`
3. 实现 `send()` 方法
4. 在 `scheduler.py` 中初始化并添加到 `notifiers` 列表

### 5.3 新增 LLM 提供商

1. 在 `llm/` 下创建新文件
2. 继承 `BaseLLM`
3. 实现 `chat()` 和 `batch_chat()` 方法
4. 在 `scheduler.py` 中根据配置选择使用

### 5.4 接入 RAG

预留扩展点：
- 在 `BaseLLM.process_news()` 中增加 RAG 检索步骤
- 配置文件增加 `rag` 配置段
- 支持多 RAG 源配置

## 6. 技术选型说明

| 组件 | 选型 | 说明 |
|-----|------|------|
| 定时任务 | APScheduler | 功能强大，支持动态修改 |
| 爬虫 | requests + BeautifulSoup | 简单易用，适合静态页面 |
| 数据库 | SQLite | MVP 阶段轻量化选择 |
| ORM | SQLAlchemy | Python 主流 ORM |
| LLM | OpenAI SDK | 兼容通用协议 |
| 配置 | Pydantic Settings + YAML | 类型安全，易于管理 |
| 日志 | loguru | 简单强大的日志库 |

